#!/usr/bin/env ruby

$: << File.expand_path(File.join(File.dirname(__FILE__), '../lib'))

require 'cycle_hire'

unless ARGV.size == 2
  abort "Usage: #{$0} username password"
end

session = CycleHire::Session.new ARGV[0], ARGV[1]
session.authenticate!
puts session.history.to_yaml
